/**
 * Application and feature flags / timings used in hooks and components.
 */

/** Duration (ms) to show initial skeleton before revealing content. */
export const SKELETON_DURATION_MS = 1500;

/** Interval (ms) at which chart data is appended during simulation. */
export const CHART_UPDATE_INTERVAL_MS = 5000;

/** Interval (ms) for the simulation elapsed timer tick. */
export const TIMER_TICK_MS = 1000;

/** Theme storage key for persistence. */
export const THEME_STORAGE_KEY = "quantexq-theme";

/** Simulation timer widget position storage key. */
export const SIMULATION_TIMER_STORAGE_KEY =
  "quantexq-simulation-timer-position";
