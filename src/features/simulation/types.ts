// Simulation feature types
export interface SimulationState {
  isRunning: boolean;
  isPaused: boolean;
  elapsedTime: number;
}

export interface SimulationData {
  // Add simulation data types here
}
